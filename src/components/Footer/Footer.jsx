import * as React from 'react';
// import { withStyles } from '@material-ui/core/styles';
// import styles from './styles';

const Footer = props => {
  return (
    <div>
      <h1>askAway: FOOTER</h1>
    </div>
  )
}

// export default withStyles(styles)(Footer);
export default Footer;